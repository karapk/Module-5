import { Html, Head, Main, NextScript } from "next/document";

export default function Document() {
  return (
    <Html lang="en">
      <Head />
      <body>
        <Main />
        <NextScript />
      </body>
    </Html>
  );
}

// The Document component is used to modify the entire HTML document.
// The Html component is used to wrap the entire document.
// The Head component is used to modify the head of the document.
// The Main component is used to wrap the main content of the document.

